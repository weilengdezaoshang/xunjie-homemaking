<view class="container" bind:tap="handleSelect">
    <i-badge show="{{showDadge}}" count="{{badgeCount}}">
        <i-icon wx:if="{{icon}}" color="#333333" name="{{icon}}" size="{{iconSize}}"></i-icon>
        <view>{{text}}</view>
    </i-badge>
</view>